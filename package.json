{
  "name": "brownie-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "biome check .",
    "clean": "rm -rf node_modules .turbo .yarn && rm -rf apps/*/.next apps/*/.turbo apps/*/node_modules apps/*/dist && rm -rf packages/*/.turbo packages/*/node_modules packages/*/dist"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "tsup": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
